import { NgxFiledropperDirective } from './ngx-filedropper.directive';

describe('NgxFiledropperDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFiledropperDirective();
    expect(directive).toBeTruthy();
  });
});
